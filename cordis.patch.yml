# dsh-v-explorer 服务端补丁层：把插件注册进 cordis loader。
# 宿主端提供 /api/dsh-v-explorer/* 路由；client-modules 发现本包的
# dsh.client 声明后自动把 lib/client.js 编入 /plugins 启动图。
- insert:
    - id: dsh-v-explorer
      name: dsh-v-explorer
